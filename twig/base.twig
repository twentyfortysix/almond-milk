{% import 'macro/wpml_switch.twig' as wpml_switch %}
<!DOCTYPE html>
<html>
    <head>
         <link rel="icon" href="{{ theme.path }}/img/icon.png" />
         <meta http-equiv="Content-Type" content="text/html;charset=utf-8" />
         <title>{{ wp_title }}</title>
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta name="code-by" content="2046.cz" />
        <!-- web coded by http://2046.cz -->
        <!-- some code licensed under Aggressive Copyright http://aggressive-copyright.org -->

        <!--[if lt IE 9]>
            <script src="{{ theme.path }}/js/vendor/respond.min.js"></script>
        <![endif]-->
        {{ wp_head }}
    </head>
    <body class="{{ body_class }}">
        {% block top_menu %}
            <nav class="navbar">
                <div class="container">
                    <div class="row">
                        <div class="col-12">
                            {% block menu %}
                               {{ function('bootstrap_menu', 'top_menu') }}
                               {{ function('bootstrap_menu', 'top_menu') }}
                            {% endblock %}
                        </div>
                    </div>
                </div>
            </nav>
        {% endblock %}

        {% block content %}
        {% endblock %}

    {{wp_footer}}
    </body>
</html>
